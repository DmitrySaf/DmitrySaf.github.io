mixin range-slider(options)
  if !options
    - const options = {}

  - const { label } = options

  .range-slider__container
    .range-slider__wrapper
      h3.range-slider__header= label
      label.range-slider__label(for="range")
    input.range#range(type="text")
