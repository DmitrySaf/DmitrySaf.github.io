mixin type(options)
  if (!options)
    - const options = {}

  -
    const {
      tag,
      text
    } = options

  .type 
    .type__tag(
      class=`type__tag_name_${tag}`
    )=tag
    .type__text(
      class=`type__text_name_${tag}`
    )=text