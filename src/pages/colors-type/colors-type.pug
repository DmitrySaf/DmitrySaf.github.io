extends ../ui-kit-template/ui-kit-template

block variables
  -
    const colors = require('./data/colors-list.json')
    const types = require('./data/types-list.json')

block page 
  section.colors-type
      .colors-type__container
        .colors-type__grid 
          .colors-type__colors
            each color in colors
              +color(color)
          .colors-type__type
            each typeitem in types
              +type(typeitem)

