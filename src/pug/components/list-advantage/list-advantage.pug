mixin list-advantage(options)
  if !options
    - var options 
  
  .advantage 
    .advantage__img
      img(src=require(`../../components/list-advantage/img/${options.imgName}.svg`), alt=options.imgName)
    .advantage__text_wrapper
      .advantage__text_header=options.header
      .advantage__text_descr=options.descr